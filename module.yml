id: "{{pkg.name}}"
version: "{{pkg.version}}"
compatibility:
  minimum: '10'
  verified: '10'
  maximum: '10'
authors:
    - name: 'Darkmoor'
      email: 'patrick.s.porto@gmail.com'
      discord: 'patrickporto#9260'

title: A/V Quality of Life
description: A Quality of Life for Foundry Audio/Video
languages:
  - lang: en
    name: English
    path: lang/en.json
esmodules:
  - avqol.mjs
styles:
  - avqol.css

url: "{{pkg.repository.url}}"
manifest: "{{pkg.repository.url}}/releases/download/{{pkg.version}}/module.json"
download: "{{pkg.repository.url}}/releases/download/{{pkg.version}}/{{pkg.name}}.zip"
readme: "{{pkg.repository.url}}/blob/main/README.md"
changelog: "{{pkg.repository.url}}/blob/main/CHANGELOG.md"
bugs: "{{pkg.repository.url}}/issues"
